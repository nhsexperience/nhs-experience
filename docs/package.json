{
  "name": "nhsexpdocs",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build-production": "JEKYLL_ENV=production webpack && bundle install && bundle exec jekyll build  --config _config.yml,_config_production.yml",
    "build-staging": "JEKYLL_ENV=staging webpack && bundle install && bundle exec jekyll build --config _config.yml,_config_staging.yml",
    "build-development": "JEKYLL_ENV=development webpack && bundle install && bundle exec jekyll build --config _config.yml,_config_development.yml",
    "debug": "JEKYLL_ENV=development bundle exec jekyll serve --config _config.yml,_config_development.yml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.74.0",
    "webpack-cli": "^4.2.0"
  },
  "dependencies": {
    "canvg": "^4.0.1",
    "jquery": "^3.6.1"
  }
}
